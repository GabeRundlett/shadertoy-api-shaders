{
    "Shader": {
        "info": {
            "date": "1676824431",
            "description": "Synthwave grid from https://www.shadertoy.com/view/7lKyDD",
            "flags": 0,
            "hasliked": 0,
            "id": "dt2SDt",
            "likes": 10,
            "name": "Synthwave grid",
            "published": 3,
            "tags": [
                "grid",
                "synthwave"
            ],
            "usePreview": 0,
            "username": "sdclibbery",
            "viewed": 314
        },
        "renderpass": [
            {
                "code": "// Original: https://www.shadertoy.com/view/7lKyDD\n\nfloat grid(vec2 uv)\n{\n  vec2 size = vec2(uv.y, uv.y * uv.y * 0.2) * 0.01;\n  uv += vec2(0.0, iTime * 4.0);\n  uv = abs(fract(uv) - 0.5);\n  vec2 lines = smoothstep(size, vec2(0.0), uv);\n  lines += smoothstep(size * 5.0, vec2(0.0), uv) * 0.4;\n  return clamp(lines.x + lines.y, 0.0, 3.0);\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord)\n{\n  vec2 uv = (2.0 * fragCoord.xy - iResolution.xy) / iResolution.y;\n  float horizon = -0.9;\n\n  float fog = smoothstep(0.2, -0.05, abs(uv.y + horizon));\n  vec3 col = vec3(0.0, 0.1, 0.2);\n  if (uv.y < -horizon)\n  {\n    uv.y = 3.0 / (abs(uv.y + horizon) + 0.05);\n    uv.x *= uv.y * 1.0;\n    float gridVal = grid(uv);\n    col = mix(col, vec3(1.0, 0.25, 0.5), gridVal);\n  }\n\n  col += fog * fog * fog;\n  col = mix(vec3(0.75, 0.1, 0.45) * 0.2, col, 0.7);\n\n  fragColor = vec4(col, 1.0);\n}\n",
                "description": "",
                "inputs": [],
                "name": "Image",
                "outputs": [
                    {
                        "channel": 0,
                        "id": 37
                    }
                ],
                "type": "image"
            }
        ],
        "ver": "0.1"
    }
}