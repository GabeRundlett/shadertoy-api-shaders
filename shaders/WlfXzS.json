{
    "Shader": {
        "info": {
            "date": "1563294069",
            "description": "How to nicely interpolate grid values to higher resolution using cardinal sinus (sinc).\nMouse = pixel to compute.\nWhite rings : nodes in the radius-s window. inner grey = weight. \nred/green = positive/negative areas.\nResult = sum ( weight * node value )",
            "flags": 0,
            "hasliked": 0,
            "id": "WlfXzS",
            "likes": 11,
            "name": "sinc magnification filter",
            "published": 3,
            "tags": [
                "filter",
                "magnification",
                "interpolation",
                "tuto",
                "sinc"
            ],
            "usePreview": 0,
            "username": "FabriceNeyret2",
            "viewed": 960
        },
        "renderpass": [
            {
                "code": "#define SCALE 6.\n#define s 3.      // sinc window size = (2s+1)^2\n#define sinc(x) sin(3.1416*(x)+1e-20) / (3.1416*(x)+1e-20)\n\nvoid mainImage( out vec4 O, vec2 U )\n{\n    vec2 R = iResolution.xy,\n         M = iMouse.xy;\n    if (length(M)<10.) M = R/2. + R/6.*cos(iTime+vec2(0,33));\n    M = SCALE* ( 2.*M - R ) / R.y;\n    U = SCALE* ( 2.*U - R ) / R.y;\n    \n    // --- draw continuous sinc function\n    vec2 D = U-M,\n         W = max(abs(D.x),abs(D.y)) <= s ? sinc(D) : vec2(0),\n         F = fract(U+.5) - .5;\n    O  = W.x*W.y * vec4(1,-1,0,0);\n    \n    // --- draw nodes\n    D = round(U)-M;\n    bool w = max(abs(D.x),abs(D.y)) <= s+.5;\n    W =  w ? sinc(D) : vec2(0);\n\n    float l = length(F),\n          p = SCALE * 2./R.y;\n    O = mix( O, vec4(abs(W.x*W.y)), smoothstep(p,0.,l-.2) ); // draw node weight\n    O = mix( O, w?vec4(1):vec4(0,.3,1,0), smoothstep(p,0.,abs(l-.2)) ); // draw node\n\n    // --- to sRGB    \n    O = pow( O, vec4(1./2.2) );\n}",
                "description": "",
                "inputs": [],
                "name": "Image",
                "outputs": [
                    {
                        "channel": 0,
                        "id": 37
                    }
                ],
                "type": "image"
            }
        ],
        "ver": "0.1"
    }
}