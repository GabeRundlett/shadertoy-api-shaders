{
    "Shader": {
        "info": {
            "date": "1508720283",
            "description": "I'm ripping paintings off of google images.",
            "flags": 0,
            "hasliked": 0,
            "id": "4tXyDs",
            "likes": 2,
            "name": "10/2/17 M O N D R I A N ",
            "published": 3,
            "tags": [
                "graprog"
            ],
            "usePreview": 0,
            "username": "L1tang",
            "viewed": 114
        },
        "renderpass": [
            {
                "code": "vec3 box(vec2 uv, vec2 pos, vec2 dimension, vec3 fill, \n         vec3 bColor, vec2 bThicc, bool bSmooth, bool borderOnly)\n{\n    if (borderOnly == true)\n    {\n        float border = step(uv.x, pos.x) - step(uv.x, pos.x + dimension.x);\n        border *= step(uv.y, pos.y) - step(uv.y, pos.y + dimension.y);\n        \n        float box = step(uv.x, pos.x + bThicc.x) - step(uv.x, (pos.x - bThicc.y) + dimension.x);\n        box *= step(uv.y, pos.y + bThicc.y) - step(uv.y, (pos.y - bThicc.y) + dimension.y);\n        \n        vec3 finalBox = mix(vec3(0.), bColor, border);\n        finalBox = mix(finalBox, vec3(0.), box);\n        return finalBox;\n    }\n    \n    else\n    {\n        float border = step(uv.x, pos.x) - step(uv.x, pos.x + dimension.x);\n        border *= step(uv.y, pos.y) - step(uv.y, pos.y + dimension.y);\n        \n        float box = step(uv.x, pos.x + bThicc.x) - step(uv.x, (pos.x - bThicc.y) + dimension.x);\n        box *= step(uv.y, pos.y + bThicc.y) - step(uv.y, (pos.y - bThicc.y) + dimension.y);\n        \n        vec3 finalBox = mix(vec3(0.), bColor, border);\n        finalBox = mix(finalBox, fill, box);\n        return finalBox;\n    }\n}\n\n\n\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n\tvec2 uv = fragCoord.xy / iResolution.xy;\n    \n    vec3 white = vec3(1,1,1); \n    vec3 red = vec3(1,0,0);\n    vec3 blue = vec3(0,0,1);\n    vec3 yellow = vec3(1,1,0);\n    vec3 black = vec3(0,0,0);\n    \n    vec3 painting = box(uv, vec2(0, 0), vec2(0.05, .3), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect = box(uv, vec2(0, .3), vec2(0.05, .3), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect2 = box(uv, vec2(0, .6), vec2(0.05, .3), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect3 = box(uv, vec2(.04, .6), vec2(0.1, .3), yellow, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect4 = box(uv, vec2(.04, .3), vec2(0.1, .3), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect5 = box(uv, vec2(.04, .15), vec2(0.1, .15), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect6 = box(uv, vec2(.04, .0), vec2(0.1, .15), red, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect7 = box(uv, vec2(.135, .6), vec2(0.2, .3), red, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect8 = box(uv, vec2(.135, .3), vec2(0.2, .3), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect9 = box(uv, vec2(.13, .15), vec2(0.3, .15), blue, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect10 = box(uv, vec2(.13, .0), vec2(0.3, .15), yellow, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect11 = box(uv, vec2(.33, .3), vec2(0.1, .3), white, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect12 = box(uv, vec2(.33, .6), vec2(0.25, .3), blue, \n                    black, vec2(.01,.01), false, false);\n    \n    vec3 rect13 = box(uv, vec2(.425, .3), vec2(0.155, .3), red, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect14 = box(uv, vec2(.425, .15), vec2(0.155, .15), white, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect15 = box(uv, vec2(.425, .0), vec2(0.24, .15), white, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect16 = box(uv, vec2(.57, .15), vec2(0.095, .15), yellow, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect17 = box(uv, vec2(.57, .3), vec2(0.095, .3), blue, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect18 = box(uv, vec2(.57, .6), vec2(0.095, .3), yellow, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect19 = box(uv, vec2(.655, .0), vec2(0.095, .15), white, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect20 = box(uv, vec2(.655, .15), vec2(0.095, .45), white, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 rect21 = box(uv, vec2(.655, .6), vec2(0.095, .3), white, \n                   black, vec2(.01,.01), false, false);\n    \n    vec3 color;\n    color += painting;\n    color += rect;\n    color += rect2;\n    color += rect3;\n    color += rect4;\n    color += rect5;\n    color += rect6;\n    color += rect7;\n    color += rect8;\n    color += rect9;\n    color += rect10;\n    color += rect11;\n    color += rect12;\n    color += rect13;\n    color += rect14;\n    color += rect15;\n    color += rect16;\n    color += rect17;\n    color += rect18;\n    color += rect19;\n    color += rect20;\n    color += rect21;\n    \n    \n\tfragColor = vec4(color,1.0);\n}\n\n",
                "description": "",
                "inputs": [],
                "name": "Image",
                "outputs": [
                    {
                        "channel": 0,
                        "id": 37
                    }
                ],
                "type": "image"
            }
        ],
        "ver": "0.1"
    }
}