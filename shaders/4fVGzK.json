{
    "Shader": {
        "info": {
            "date": "1712987071",
            "description": ":)",
            "flags": 0,
            "hasliked": 0,
            "id": "4fVGzK",
            "likes": 14,
            "name": "➿layered",
            "published": 3,
            "tags": [
                "2d",
                "trippy"
            ],
            "usePreview": 0,
            "username": "scry",
            "viewed": 206
        },
        "renderpass": [
            {
                "code": "////////////////////////////////////////////////////////////\n////////////////////////////////////////////////////////////////////////////////\n#define pi acos(-1.)\n#define deg pi/180.\n#define R iResolution.xy\n#define ar R.x/R.y\n#define time iTime*pi/100.\nvec3 cs = vec3(1.,2.,3.);\n\nmat2 r2d(float a) {\n    return mat2(cos(a),sin(a),-sin(a),cos(a));\n}\nvec3 c1(vec2 uv, float tt) {\n    \n    vec3 col = vec3(0.);\n    uv = uv/dot(uv,uv)/100.;\n    //uv.xy *= r2d(time);\n    \n    uv += vec2(sin(tt),cos(tt))*0.04;\n    \n    //uv += vec2(sin(time),cos(time+0.5))*0.04;\n    //uv += sin(length(uv)+time)*0.2;\n    uv = uv/dot(uv,uv)/100.;\n    //uv = abs(uv);\n    vec2 pv = uv;\n    uv = fract(uv*8.)-0.5;\n    uv = abs(uv)-0.25;\n    uv = mix(uv,pv,sin(length(uv)*9.)*0.1+0.95);\n    uv = uv/dot(uv,uv)/100.;\n    //uv.xy *= r2d(time);\n    \n    for (int i=0;i<8;i++) {\n        uv += vec2(sin(tt+uv.x*4.),cos(tt+uv.y*4.))*0.01;\n    }\n    uv = uv/dot(uv,uv)/100.;\n    uv = fract(uv*8.);\n    uv -= 0.5;\n    uv = abs(uv)-0.25;\n    float t = tt*10.;\n    uv *= r2d(time*20.);\n    vec2 cv = vec2(length(uv),atan(uv.x,uv.y));\n    col += smoothstep(0.01,0.,length(uv+vec2(0.,0.2)));\n    float w = max(abs(cv.x-0.2),cv.y*0.1+0.1);\n    col += smoothstep(0.2+cv.y*0.063,0.,w);\n    col += sin(cs+w*200.+time*-300.)*0.05;\n    col = clamp(col,0.,1.);\n    return col;\n}\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord)\n{\n    vec2 uv = fragCoord.xy / iResolution.xy;\n    vec2 tv = uv;\n    uv -= 0.5;\n    uv.x *= ar;\n    vec3 col = vec3(0.);\n    float c = length(uv);\n    float rt = sin(time/25.)*20.;\n    for (int i=0;i<60;i++) {\n        rt = sin(time/10.+c*sin(time+float(i))*0.005+float(i)*pi*0.5)*20.;\n        col += c1(uv,time+float(i)*-0.002)/19.*(sin(cs*1.1+float(i)+time*20.)*0.8+0.4);\n        uv += vec2(sin(uv.y*40.+time),cos(uv.x*40.+time))*0.0004;\n        uv *= r2d(rt*1.+sin(rt*100.+c*0.01)/100.);\n        //uv = uv/dot(uv,uv);\n        uv *= 1.+sin(rt*2.)*0.03;\n    }\n    //col *= 1.1;\n    //col -= 0.2;\n    fragColor = vec4(col,1.0);\n}",
                "description": "",
                "inputs": [],
                "name": "Image",
                "outputs": [
                    {
                        "channel": 0,
                        "id": 37
                    }
                ],
                "type": "image"
            }
        ],
        "ver": "0.1"
    }
}