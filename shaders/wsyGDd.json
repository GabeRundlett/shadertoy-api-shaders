{
    "Shader": {
        "info": {
            "date": "1571196409",
            "description": "Infinite fractal version of u/MigueLozano's yin yang on reddit: https://www.reddit.com/r/woahdude/comments/di670t/double_yin_yang_animation_i_coded_some_time_ago/",
            "flags": 0,
            "hasliked": 0,
            "id": "wsyGDd",
            "likes": 7,
            "name": "Yin Yang Infinite Zoom",
            "published": 3,
            "tags": [
                "fractal",
                "zoom"
            ],
            "usePreview": 0,
            "username": "jaburns",
            "viewed": 489
        },
        "renderpass": [
            {
                "code": "#define ITERATIONS 25\n#define time (mod((.4*iTime), 2.*3.14159265))\n\nvec2 rot(vec2 p, float t) {\n    return mat2(cos(t), -sin(t), sin(t), cos(t)) * p;\n}\n\nfloat yinYang(vec2 p)\n{\n    for (int i = 0; i < ITERATIONS + 1; ++i) {\n        p = rot(p, time*(i % 2 == 1 ? 1. : -2.));\n\n        float yin = p.y > 0.\n            ? 1.\n            : 0.;\n\n        vec2 p1 = p.y > 0.\n            ? p-vec2(0,.5)\n            : p+vec2(0,.5);\n\n        float d = length(p1);\n\n        if (d < .5) {\n            if (i >= ITERATIONS) {\n                if (d < .2) return 1. - yin;\n                return yin;\n            }\n            p = 2.*p1;\n            continue;\n        }\n    \n        return p.x > 0. ? 1. : 0.;\n    }\n}\n\nvec2 cam()\n{\n    vec2 p = vec2(0, .5);\n    vec2 o = vec2(0);\n    \n    for (int i = 0; i < ITERATIONS; ++i) {\n        p = rot(p, -time*(i % 2 == 1 ? 1. : -2.));\n        o += p;\n        p /= -2.;\n    }\n\n    return o;\n}\n\nvec2 getUV(vec2 c, vec2 fragCoord, vec2 d)\n{  \n\tvec2 uv = (fragCoord + .5*d)/iResolution.yy - vec2(.5*iResolution.x/iResolution.y,.5);\n    uv = rot(uv, 3.14159/2.);    \n    uv /= pow(3., 1.+time);\n    uv += c;\n    return uv;\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    vec2 c = cam();\n    \n    float sampleA = yinYang(getUV(c, fragCoord, vec2(-.75, .25)));\n    float sampleB = yinYang(getUV(c, fragCoord, vec2(-.25,-.75)));\n    float sampleC = yinYang(getUV(c, fragCoord, vec2( .25, .75)));\n    float sampleD = yinYang(getUV(c, fragCoord, vec2( .75,-.25)));\n    float aa = .25*(sampleA + sampleB + sampleC + sampleD);\n\n    fragColor = vec4(aa);\n}",
                "description": "",
                "inputs": [],
                "name": "Image",
                "outputs": [
                    {
                        "channel": 0,
                        "id": 37
                    }
                ],
                "type": "image"
            }
        ],
        "ver": "0.1"
    }
}