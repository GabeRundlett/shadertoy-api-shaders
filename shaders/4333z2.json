{
    "Shader": {
        "info": {
            "date": "1716735852",
            "description": "yes",
            "flags": 0,
            "hasliked": 0,
            "id": "4333z2",
            "likes": 9,
            "name": "live coding training 01",
            "published": 3,
            "tags": [
                "training"
            ],
            "usePreview": 0,
            "username": "rcargou",
            "viewed": 168
        },
        "renderpass": [
            {
                "code": "mat2 ro(float a) {\n    return mat2(cos(a), -sin(a), sin(a), cos(a));\n}\n\nfloat sp(vec3 pp) {\nfloat ret = 0.;\nfor (int i = 0; i < 1; ++i) {\nvec3 p = pp;\nfloat rz = 5.;\nfloat aa = floor((p.z + 13.*iTime) * rz);\nfloat tt = mix(.3, 1.9, 1.+.5*sin(iTime / 2.) );;\ntt = 2.1;\n//p.y += sin(p.z / 100.) * 2.5;\np.z = mod(p.z + 13.*iTime, rz) - rz / 2.;\n    p.z -= 2.5;\n        p.xy *= ro(aa / 12. + tt / 2. + (pp.z + iTime * 13.) * 1.3);\n\n    float rx = .3;\n    p.x = mod(p.x, rx) - rx / 2.;\n    p.y = abs(p.y);\n//    p.y -= 1.9;\n    p.y -= tt;\n    ret += (length(p) + .01);\n    }\n    return ret;\n\n}\nfloat map(vec3 p) {\n    return min(sp(p), sp(p.xyz));\n}\n\n\nfloat rm(vec3 ro, vec3 rd) {\n    float t = 0.;\n    float ret = .0;\n    for (int i = 0; i < 180;++i) {\n        vec3 p = ro + rd * t;\n\n        float d = map(p);\n        t += d * .35;\n        ret += .0025/d;//smoothstep(.1, .0, d);\n    }\n    return ret;\n}\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = (fragCoord - iResolution.xy /2.) / iResolution.xx;\n    \n    vec3 rd = normalize(vec3(uv, 1.));\n    // Time varying pixel color\n    float c = rm(vec3(0.), rd);\n    \n    vec3 col = vec3(c * 2., c, c * 2. + .2*sin(iTime) );//vec3( .2*sin(c * 5.+ iTime * 1.), c, c  );\n\n    // Output to screen\n    fragColor = vec4(col * 1.5,1.0);\n}",
                "description": "",
                "inputs": [],
                "name": "Image",
                "outputs": [
                    {
                        "channel": 0,
                        "id": 37
                    }
                ],
                "type": "image"
            }
        ],
        "ver": "0.1"
    }
}